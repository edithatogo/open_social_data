# Data dictionary

The bounded provider emits `record_id`, `source_url`, `record_type`,
`capture_scope`, `dataset_id`, and `provider`. It intentionally does not emit
biographies, documents, or media in the sample dataset.
